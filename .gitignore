/target
/data
/results
/.venv
/tools/llama.cpp
/seeds
*.code-workspace
crash-*

논문 관련/
미래내일/
*-seeds.tar.gz

__pycache__/

# internal/private docs
AGENTS.md
CLAUDE.md
# root-anchored: an unanchored `.github/` also matched vendor/*/.github, which
# excluded 27 files that the crates' .cargo-checksum.json lists -> `cargo build
# --offline` failed on every fresh clone while the dev machine kept working.
/.github/
.claude/
docs/
docs/dev-todo.md
docs/rules.md
docs/dev-todo-legacy-upgrade.md
docs/experiment-ops.md
docs/progress-log.md
docs/dashboard-phase-a.md
docs/format-scope-roadmap.md
docs/legacy/
docs/plans/
.codex/

# demo recording staging (never commit, delete after filming)
demo-stage/

# native fuzz harness build artifacts
/harnesses/libfuzzer/onnxruntime_loader_fuzzer
/harnesses/libfuzzer/onnxruntime_loader_replay
/harnesses/libfuzzer/gguf_loader_fuzzer
/harnesses/libfuzzer/gguf_loader_replay
/harnesses/libfuzzer/safetensors_loader_fuzzer
/harnesses/libfuzzer/safetensors_loader_replay
/harnesses/aflpp/safetensors_loader_replay

# safetensors Rust fuzz crate build output (vendor/ IS committed for offline reproducibility)
/fuzz/target
/fuzz/target-aflpp
/harnesses/aflpp/
/fuzz/target-cov

# coverage profiles (never commit; vendor/ is committed)
*.profraw
*.profdata

# R55: staging files a build leaves behind when it is killed (SIGKILL/OOM) before its EXIT
# trap can run. harnesses/aflpp/ is ignored as a whole directory above; the libFuzzer
# binaries are listed one by one, so their staging names need their own rule.
# Appended at the end on purpose: inserting mid-file shifts every line number below it,
# and BASE-01 cases cite this file by line (that is how two citations went stale once).
/harnesses/libfuzzer/*.new.*
